<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorBackground">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>!-->
        <!--<item name="colorAccent">@color/accent_material_dark</item>
        <item name="android:statusBarColor">@color/dim_foreground_material_light</item>
        <item name="android:navigationBarColor">@color/dim_foreground_material_light</item>!-->
        <!--<item name="colorAccent">@color/topBarColor</item>!-->
        <item name="colorAccent">@color/blueBt</item>
        <item name="android:windowBackground">@color/mainBg</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
